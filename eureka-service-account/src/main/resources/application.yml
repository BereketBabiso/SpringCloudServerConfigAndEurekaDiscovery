spring:
  application:
    name: account-service
server:
  port: 8095
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8085/eureka
  instance:
    hostname: localhost
   